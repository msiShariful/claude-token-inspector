# Claude Token Inspector

A Claude Code plugin that helps you understand and optimize your context window usage.

## What it does

- **`/token-inspector:inspect-context`** — Full breakdown of what's consuming your context window (files, history, skills, tools) with token estimates
- **`/token-inspector:find-bloat`** — Identifies large context items that aren't relevant to your current task

## Installation

### Add the marketplace